eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
  instance:
    instanceId: ${spring.application.name}:${random.value}
server:
  port: 8095
spring:
  application:
    name: api-gateway
